services:
  - type: web
    name: oyak-ai-1
    env: python
    buildCommand: pip install -r requirements.txt && python ingest.py
    startCommand: gunicorn server:app --workers 1 --threads 4 --timeout 300 --keep-alive 5
    envVars:
      - key: GROQ_API_KEY
        sync: false
